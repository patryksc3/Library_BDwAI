@model Library_BDwAI.Models.Loan
@{
    ViewData["Title"] = "Wypo¿ycz ksi¹¿kê";
}

<div class="container mt-4">
    <h1>Wypo¿ycz ksi¹¿kê</h1>
    <hr />

    <div asp-validation-summary="All" class="text-danger mb-3"></div>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    <form asp-action="AddLoan" method="post">

        <div class="mb-3">
            <label asp-for="UserId" class="form-label">U¿ytkownik</label>
            <select asp-for="UserId" class="form-control" asp-items="ViewBag.UsersList">
                <option value="">-- Wybierz u¿ytkownika --</option>
            </select>
            <span asp-validation-for="UserId" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="BookId" class="form-label">Ksi¹¿ka</label>
            <select asp-for="BookId" class="form-control" asp-items="ViewBag.BooksList">
                <option value="">-- Wybierz ksi¹¿kê --</option>
            </select>
            <span asp-validation-for="BookId" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="ReturnDate" class="form-label"></label>
            <input asp-for="ReturnDate" type="date" class="form-control" />
            <span asp-validation-for="ReturnDate" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary">Zarejestruj wypo¿yczenie</button>
        <a asp-controller="Loans" asp-action="Index" class="btn btn-secondary">Anuluj</a>
    </form>
</div>